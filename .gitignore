.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.env
node_modules
dist
.vercel
.env*.local

# Local-only — IDE state and large reference materials
.claude/
reports/
MDL Outline.*

# Westlaw CSV exports — kept local only (ToS + session URLs in rows).
# Ingest path: api/_tcpa-sources/westlaw-csv.js
*.csv

# Local-only data and reference material — never push (credit-matches holds real-person PII)
data/credit-matches/
data/raw/
tools/__pycache__/
docs/backups/
Frank Docs/
*.pdf
